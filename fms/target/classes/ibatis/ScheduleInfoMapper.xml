<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.sinosoft.core.domain.model.quartz.ScheduleInfoMapper">
    <select id="findByID" parameterType="int" resultType="Scheduleinfo">
        SELECT *   
        FROM ScheduleInfo  
        WHERE id = #{id}  
    </select>     
      <select id="findMapByID" parameterType="int" resultType="java.util.HashMap">
        SELECT *   
        FROM ScheduleInfo  
        WHERE id = #{id}  
    </select>   
</mapper>  