<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.sinosoft.core.domain.model.user.UserMapper">
    <select id="findByID" parameterType="int" resultType="User">
        SELECT *   
        FROM tcUser  
        WHERE id = #{id}  
    </select>  
  
     <!-- <insert id="save" parameterType="com.sinosoft.core.domain.model.user.User" useGeneratedKeys="true"  
        keyProperty="id">  
        insert into  
        tcUser(id,usercode,username,password,company_id)  
        values(SEQ_TCUSER.NEXTVAL,#{usercode},#{username},#{password},#{companyId})  
    </insert>  --> 
  
   <insert id="save" parameterType="com.sinosoft.core.domain.model.user.User" >  
        insert into  
        tcUser(id,usercode,username,password,company_id)  
        values(SEQ_TCUSER.NEXTVAL,#{usercode},#{username},#{password},#{companyId})  
    </insert> 
</mapper>  